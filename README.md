<div align="right">
  <strong>English</strong> | <a href="#简体中文">简体中文</a>
</div>

<a id="english"></a>
# 🌌 StarGate-OS: The Core Monolith

**The central nervous system of the Open-StarGate empire.** This repository houses the monolithic architecture that unifies the "Black Light Factory" (Backend Order) and the "StarGate OS" (Frontend Chaos). It is engineered for extreme hardware environments (e.g., legacy x86 nodes without VRAM), prioritizing structural control and architectural elegance over bloated frameworks.

## 🧬 Architectural Topology

### 1. The Gateway & Darwinian Matrix (Backend)
* **API Router (`main.py`)**: The central hub handling atomic-level streaming, seamless state flushing, and immune to concurrency deadlocks.
* **The Caliper Engine (`api_caliper.py`)**: Abandons static system prompts. When the external sandbox detects an execution anomaly, this engine wakes a high-tier diagnostic model to extract logical patches (DNA), dynamically injecting them into the workflow to achieve adaptive evolution.

### 2. Visual Downscaling & Native Canvas (Frontend)
* **Streaming State Collapse**: Rejects heavy frontend armors like React Flow. Uses pure Vanilla JS to intercept massive LLM entity tags. The UI physically shields the rendering process during high-frequency data streams, collapsing into a stable state only upon payload closure.
* **DAG Blueprint Generator**: Built with native SVG cubic Bézier curves, ensuring smooth rendering of complex node connections directly at the browser's lowest level, achieving massive performance gains.

## ⚠️ Architect's Disclaimer (Archived)
This repository is a snapshot of an architectural deduction. **I am not a developer.** There are NO one-click deployment scripts, NO technical support, and NO pull requests accepted. The codebase is entirely generated by commanding an AI through text-based micro-manipulation. Extract the blueprints and consult your own AI if you seek to understand the underlying philosophy.

<br><br>

---

<div align="right">
  <a href="#english">English</a> | <strong>简体中文</strong>
</div>

<a id="简体中文"></a>
# 🌌 StarGate-OS (星门总厂)：巨型单体中枢

**Open-StarGate 赛博帝国的中枢神经系统。**
本仓库收容了将“黑灯工厂（后端秩序）”与“星门 OS（前端混沌）”高度缝合的巨型单体架构。它专为极端的边缘物理环境（如十五年前无独立显存的老旧 x86 节点）打造，拒绝臃肿的现代化框架，追求极致的底层架构微操。

## 🧬 核心拓扑解析

### 1. 纯净网关与达尔文演化矩阵 (后端)
* **总控中枢 (`main.py`)**：负责 API 级联路由、无痕垃圾回收与流式数据落盘，免疫并发锁死与 RAM 溢出。
* **游标卡尺引擎 (`api_caliper.py`)**：彻底抛弃固化的超长系统提示词。当外部算力舱传回异常快照时，唤醒高维诊断模型提取“逻辑补丁 (DNA)”，动态注入业务节点，实现系统的活体自适应演化。

### 2. 视觉降维打击与原生无尽画布 (前端)
* **流式状态塌缩**：抛弃 React 等重型装甲，采用纯 Vanilla JS。在截获大模型巨量的结构化实体标签时，前端实施“流式视觉遮罩”，在数据流闭合瞬间完成状态塌缩，彻底根除 DOM 闪烁。
* **DAG 装配车间**：使用纯原生 SVG 三次贝塞尔曲线进行物理距离计算与节点连线，直穿浏览器底层渲染机制，同屏百节点丝滑流转。

## ⚠️ 厂长声明 (Archived)
本仓库为个人架构推演的定格快照。**本人非科班技术人员。**
本项目拒接任何形式的技术支持，不提供一键运行包，谢绝 Issue 与 PR。全库底层源码皆为本人以纯文本微操指挥 AI 意志代笔落盘的产物。如遇图纸晦涩，请自行提取源码并拷问你自己的 AI 助手。
