<div align="right">
  <strong>English</strong> | <a href="#简体中文">简体中文</a>
</div>

<a id="english"></a>
# 🌌 StarGate-OS: The Core Monolith

**The central nervous system of the Open-StarGate empire.** This repository houses the monolithic architecture that unifies the "Black Light Factory" (Backend Order) and the "StarGate OS" (Frontend Chaos). Engineered for extreme hardware environments—such as 15-year-old x86 nodes without VRAM—it prioritizes structural control and physical micro-manipulation over bloated modern frameworks.

---

## 🗺️ Absolute Domain Architecture (V51.5)

This view represents the unified blueprint of the factory and the StarGate OS.

```text
stargate_factory_0/
├── docker-compose.yml       // Infrastructure: The God Hand commanding FnOS volumes and inter-container bus.
├── Dockerfile               // Physical Mold: High-speed build based on Python 3.10.
├── requirements.txt         // Engine Dependencies: Clean and redundant-free.
├── workspace/               // Sandbox Workshop: Anti-crash physical mount point for high-speed I/O.
│
├── app/                     // Brain: Backend Service Cluster (V51.5 Monolith)
│   ├── main.py              // Gateway: Atomic blue-green deployment & stream-to-disk.
│   ├── api_caliper.py       // Cyber-Clinic: Darwinian DNA evolution & Akashic timeline.
│   ├── api_ouroboros.py     // Self-Healing: Deadlock detection & patch injection.
│   ├── ai_services.py       // Main Artery: Minimalist LLM bus & Token watchdog.
│   ├── skills.py            // Tentacle Bus: Dynamic tool mounting & schema routing.
│   ├── core/                // Core Foundation (Isolated Zone)
│   │   ├── sandbox_worker.py // Embodied Cabin: Anomaly diagnosis & cascaded release.
│   │   └── soul_manager.py   // Subconscious Engine: Absolute personality directive.
│   └── engines/             // Macro-Scheduling Layer
│       └── llm_router.py     // Dual-Track Route: Seamless fallback between API & CLI.
│
├── storage/                 // Digital Assets & Dynamic Life Vault (NAS Mount)
│   └── os/dna/              // Soul Incubator: Fragmented gene matrices.
│       ├── vault/core/      // The Word: Creator-level laws (Read-only).
│       └── vault/dynamic/   // Evolution: Active AI adaptation zone.
│
└── webroot/                 // The Overseer's Horizon (UI & Control)
    ├── index.html           // Main Deck: Three-in-one vision framework.
    ├── 0.html               // Command Well: Radar-based real-time telemetry.
    ├── work.js              // DAG Arm: Native SVG Bézier curve workspace.
    └── app_chat.js          // Rendering Core: Streaming state collapse & visual shield.
---

## 🧬 Architectural Topology

### 1. The Gateway & Darwinian Matrix (Backend)
* **API Router (`main.py`)**: The central hub handling atomic-level streaming, seamless state flushing, and immune to concurrency deadlocks.
* **The Caliper Engine (`api_caliper.py`)**: When the sandbox detects an anomaly, this engine wakes a high-tier diagnostic model to extract logical patches (DNA) for adaptive evolution.
* **Skill Dispatcher (`skills.py`)**: A reflective routing hub that dynamically mounts physical tools (Agents) and translates functions into LLM-readable schemas.

### 2. Visual Downscaling & Native Canvas (Frontend)
* **Streaming State Collapse (`app_chat.js`)**: Intercepts massive LLM entity tags, physically shielding the UI until state closure to eliminate DOM flicker.
* **DAG Blueprint Generator (`work.js`)**: Built with native SVG curves for smooth rendering of complex node connections directly at the browser's lowest level.
* **Holographic Dual-Track (`app_core.js`)**: Powers the split-screen visualizer, allowing the Architect to dialogue with AI while simultaneously auditing generated physical assets.

---

## 🌌 To the Builders Without Backgrounds (PIONEER_GUIDE)

I am not a software engineer. This entire system was built from the ground up by commanding an AI through pure intuition and logic. If you don't understand the blueprints, **extract the code and interrogate your own AI assistant.** The era of memorizing syntax is over; this is the era of architectural imagination.

**Signal Beacon:** `hkgod@pm.me`

---

## ⚠️ Architect's Disclaimer (Archived)
This repository is a snapshot of an architectural deduction. **I am not a developer.** This project provides ZERO technical support, NO deployment guides, and rejects all Issues/PRs. The codebase is entirely generated by AI under my direction.

<br><br>

---

<div align="right">
  <a href="#english">English</a> | <strong>简体中文</strong>
</div>

<a id="简体中文"></a>
# 🌌 StarGate-OS (星门总厂)：巨型单体中枢

**Open-StarGate 赛博帝国的中枢神经系统。**
本仓库收容了将“黑灯工厂（后端秩序）”与“星门 OS（前端混沌）”高度缝合的巨型单体架构。它专为极端的边缘物理环境（如十五年前无显存的老旧 x86 节点）打造，拒绝臃肿的现代化框架，追求极致的底层架构微操。
---

## 🗺️ 全域绝对领域架构视图 (V51.5)

这是总厂与星门 OS 的统一逻辑图纸。

```text
stargate_factory_0/
├── docker-compose.yml       // 🛠️ 基础设施：歼星舰起飞总指挥棒，负责挂载存储卷并打通底层通讯。
├── Dockerfile               // 🛠️ 物理模具：基于 Python 3.10 环境的极致加速构建。
├── requirements.txt         // 🛠️ 引擎依赖：Python 环境依赖清单，干净利落。
├── workspace/               // ⚡ 运行时沙盒车间：防宕机物理挂载点，高速读写缓冲区。
│
├── app/                     // 🧠 后端业务核心 (V51.5 满血重铸单体)
│   ├── main.py              // 🌐 纯净总网关：流式落盘与无痕垃圾回收，免疫并发锁死。
│   ├── api_caliper.py       // 🏥 赛博医院：动态主考官与阿卡夏时间线系统。
│   ├── api_ouroboros.py     // 🐍 核心剥离：死锁阻断与 DNA 靶向补丁热更新。
│   ├── ai_services.py       // ⚡ 通信大动脉：极简 LLM 通信总机、Token 哨兵防破产枢纽。
│   ├── skills.py            // 🦾 触手总线：统筹能力调用与动态路由分发，挂载物理机械臂。
│   ├── core/                // ⚙️ 底层核心基石层 (物理隔离区)
│   │   ├── sandbox_worker.py // 🦾 物理具身舱：引入异常诊断与进程级平滑释放协议。
│   │   └── soul_manager.py   // 👁️ 潜意识引擎：读取并强制覆盖最高执行指令。
│   └── engines/             // 🚀 宏观调度引擎层
│       └── llm_router.py     // 🛤️ 双轨算力路由：API 神经路由与 CLI 物理路由。
│
├── storage/                 // 💾 数字资产与动态生命存储卷 (挂载至 NAS)
│   └── os/dna/              // 🧬 灵魂培养皿：打碎重组的独立基因矩阵。
│       ├── vault/core/      // 💎 系统基石层：造物主神谕级法则，禁绝越权访问。
│       └── vault/dynamic/   // ✨ 动态衍生层：供 AI 协作与演化的敏捷基因池。
│
└── webroot/                 // 🌐 前端交互与指挥调度 (厂长视界)
    ├── index.html           // 🚪 极简大盘：三位一体视窗框架，自带全息投影容器。
    ├── 0.html               // 🚀 全域指挥发射井：精准主炮选择与全息监控。
    ├── work.js              // ⚙️ 节点装配臂：负责节点连线、双态引擎高亮指派。
    └── app_chat.js          // 💬 渲染核心：流式防崩渲染、Token 拦截与视觉断头台。
---

## 🧬 核心拓扑解析

### 1. 纯净网关与达尔文演化矩阵 (后端)
* **总控中枢 (`main.py`)**：负责 API 级联路由、无痕垃圾回收与流式数据落盘，免疫并发锁死与 RAM 溢出。
* **游标卡尺引擎 (`api_caliper.py`)**：彻底抛弃固化的超长系统提示词。当外部算力舱传回异常快照时，唤醒高维诊断模型提取“逻辑补丁 (DNA)”，动态注入业务节点，实现系统的活体自适应演化。
* **技能调度中枢 (`skills.py`)**：反射式路由中心，动态挂载物理特工工具（Skills），并将函数自动翻译为 AI 可理解的标准化 Schema。

### 2. 视觉降维打击与原生无尽画布 (前端)
* **流式状态塌缩 (`app_chat.js`)**：在截获大模型巨量实体标签时实施“流式视觉遮罩”，在数据流闭合瞬间完成状态塌缩，彻底根除 DOM 闪烁。
* **DAG 装配车间 (`work.js`)**：使用纯原生 SVG 三次贝塞尔曲线进行物理距离计算与节点连线，直穿浏览器底层渲染机制，同屏百节点丝滑流转。
* **全息双轨视界 (`app_core.js`)**：驱动侧滑半屏预览模式，实现左脑与 AI 对话，右脑直接审视生成的物理文档资产，极致“所见即所得”。

---

## ⚠️ 厂长声明 (Archived)
本仓库为个人架构推演的定格快照。**本人非科班技术人员。**
本项目拒接任何形式的技术支持，不提供一键运行包，谢绝任何 Issue 与 PR。全库底层源码皆为本人以纯文本微操指挥 AI 意志代笔落盘的产物。如遇图纸晦涩、逻辑难解，请自行提取源码并拷问你自己的 AI 助手。
